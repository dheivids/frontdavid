

describe('Aprendiendo jasmine' , () => {

     it('should be 1 if n=1', () =>{
         const n = 1; 
         expect(factorial (n)).toEqual(1)


     })

     it ('should de 0 if n=0', () => {

     })

     it ('should be 120 if n=5', () => {

     })
     it('should throw error if n=5', () => {

     })
});